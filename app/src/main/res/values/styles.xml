<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/categoryButtonColor</item>
    </style>

    <style name="categoryButton" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/categoryButtonColor</item>
        <item name="android:colorButtonNormal" tools:targetApi="lollipop">@color/categoryButtonColor</item>
        <item name="colorControlHighlight">@color/categoryButtonColor</item>
        <item name="cornerRadius">20dp</item>
    </style>

    <style name="customTabLayout" parent="Base.Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyTabTextAppearance</item>
    </style>

    <style name="MyTabTextAppearance" parent="TextAppearance.AppCompat.Button">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
